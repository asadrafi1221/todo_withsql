<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        form {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
            display: grid;
            gap: 10px;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            color: #fff;
            font-size: 16px;
        }

        #editBtn {
            background-color: #f39c12;
        }

        #deleteBtn {
            background-color: #e74c3c;
        }

        .submit {
            background-color: #3498db;
        }

        button:hover {
            opacity: 0.8;
        }

        .container {
            margin-top: 20px;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        .container p {
            margin: 0;
            padding: 10px;
            background: #ecf0f1;
            border-radius: 4px;
            border: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <h1><%= data[3].name %></h1>
    <h1>helo lka</h1>
    <form id="myForm">
        <input type="text" placeholder="email" id="email" />
        <input type="text" placeholder="create your todo" id="todo" />
        <input type="text" placeholder="add your description" id="description" />
        <button type="button" id="editBtn">edit me</button>
        <button type="button" id="deleteBtn">delete me</button>
        <button type="submit" class="submit">submit your answer</button>
    </form>
    <div class="container"></div>
    <script>
        document.querySelector(".submit").addEventListener("click", async (event) => {
            event.preventDefault();  // Prevent default form submission behavior

            alert("helo");

            try {
                let response = await fetch("/get_data");
                let json_data = await response.json();
                console.log(json_data);

                let email = document.querySelector("#email").value;
                let description = document.querySelector("#description").value;

                let userFound = false;

                for (let i = 0; i < json_data.length; i++) {
                    if (email === json_data[i].email) {
                        json_data[i].posts = description;
                        userFound = true;
                        break;
                    }
                }

                if (!userFound) {
                    throw new Error("User not found");
                }

                let postobj = {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json",
                    },
                    body: JSON.stringify({
                        email: email,
                        post: description,
                    }),
                };

                response = await fetch("http://localhost:4000/userpost", postobj);

                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                console.log("Request successful");

                // Clear previous content and display updated names
                const container = document.querySelector(".container");

for (let i = 0; i < json_data.length; i++) {
    const div = document.createElement('div');
    
    const para = document.createElement('p');
    para.innerHTML = 'helo bye';  
    
    div.appendChild(para);
    
    container.appendChild(div);
}


            } catch (err) {
                console.error("Sorry, unable to post data", err);
            }
        });
    </script>
</body>
</html>
